# Authentication Flow - Visual Diagram

## 🔐 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                                 │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  User Opens  │
│  Login Page  │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Login.js Component                                               │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  State:                                                     │  │
│  │  - formData: { email, password }                           │  │
│  │  - isSubmitting: false                                     │  │
│  │  - errors: {}                                              │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       │ User enters credentials
       │ and clicks "Sign In"
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  handleSubmit()                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  1. e.preventDefault()                                     │  │
│  │  2. validateForm() → Check email & password format         │  │
│  │  3. setIsSubmitting(true)                                  │  │
│  │  4. const result = await login(formData) ← Call AuthContext│  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  AuthContext.login()                                              │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  1. dispatch({ type: LOGIN_START })                        │  │
│  │     → isLoading = true                                     │  │
│  │                                                            │  │
│  │  2. const response = await authAPI.login(credentials)      │  │
│  │     ↓                                                      │  │
│  │     POST /api/auth/login                                   │  │
│  │     { email, password }                                    │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Backend API (Node.js + Express)                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  1. Validate email format                                  │  │
│  │  2. Find user in database                                  │  │
│  │  3. Check if user.isActive                                 │  │
│  │  4. Verify password (bcrypt)                               │  │
│  │  5. Generate JWT tokens:                                   │  │
│  │     - accessToken (expires in 1 hour)                      │  │
│  │     - refreshToken (expires in 7 days)                     │  │
│  │  6. Save refreshToken to user document                     │  │
│  │  7. Update lastLogin timestamp                             │  │
│  │  8. Create login notification                              │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Response:                                                        │
│  {                                                                │
│    success: true,                                                 │
│    message: "Login successful",                                   │
│    data: {                                                        │
│      accessToken: "eyJhbGc...",                                   │
│      refreshToken: "eyJhbGc...",                                  │
│      user: {                                                      │
│        id: "...",                                                 │
│        name: "Admin User",                                        │
│        email: "admin@example.com",                                │
│        role: "Admin",                                             │
│        permissions: ["read", "write", "delete"],                  │
│        lastLogin: "2026-02-08T..."                                │
│      }                                                            │
│    }                                                              │
│  }                                                                │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  AuthContext.login() - Response Handling                          │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  if (response.success) {                                   │  │
│  │    const { accessToken, refreshToken, user } = response.data│ │
│  │                                                            │  │
│  │    // CRITICAL: Store tokens FIRST                        │  │
│  │    localStorage.setItem('accessToken', accessToken)       │  │
│  │    localStorage.setItem('refreshToken', refreshToken)     │  │
│  │                                                            │  │
│  │    // THEN update state                                   │  │
│  │    dispatch({                                             │  │
│  │      type: LOGIN_SUCCESS,                                 │  │
│  │      payload: { user }                                    │  │
│  │    })                                                     │  │
│  │    → isAuthenticated = true                               │  │
│  │    → user = userData                                      │  │
│  │    → isLoading = false                                    │  │
│  │                                                            │  │
│  │    toast.success(`Welcome back, ${user.name}!`)           │  │
│  │    return { success: true, user }                         │  │
│  │  }                                                         │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Login.js - After login() returns                                 │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  if (result.success) {                                     │  │
│  │    // CRITICAL: Wait for state propagation                │  │
│  │    await new Promise(resolve => setTimeout(resolve, 100))  │  │
│  │                                                            │  │
│  │    // Navigate based on role                              │  │
│  │    if (result.user.role === 'Admin') {                    │  │
│  │      navigate('/admin/dashboard', { replace: true })      │  │
│  │    } else {                                                │  │
│  │      navigate('/user/dashboard', { replace: true })       │  │
│  │    }                                                       │  │
│  │  }                                                         │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  React Router Navigation                                          │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  1. URL changes to /admin/dashboard                        │  │
│  │  2. React Router matches route                             │  │
│  │  3. Renders <ProtectedRoute> wrapper                       │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ProtectedRoute Component                                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  const { isAuthenticated, isLoading, user } = useAuth()    │  │
│  │                                                            │  │
│  │  // Check 1: Is loading?                                  │  │
│  │  if (isLoading) {                                          │  │
│  │    return <LoadingSpinner />                              │  │
│  │  }                                                         │  │
│  │                                                            │  │
│  │  // Check 2: Is authenticated?                            │  │
│  │  if (!isAuthenticated) {                                   │  │
│  │    return <Navigate to="/login" replace />                │  │
│  │  }                                                         │  │
│  │                                                            │  │
│  │  // Check 3: Has required role?                           │  │
│  │  if (requiredRoles.length > 0) {                          │  │
│  │    if (!hasAnyRole(requiredRoles)) {                      │  │
│  │      return <AccessDenied />                              │  │
│  │    }                                                       │  │
│  │  }                                                         │  │
│  │                                                            │  │
│  │  // All checks passed!                                    │  │
│  │  return <>{children}</>                                   │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ✅ SUCCESS: Dashboard Rendered                                   │
│  User is now logged in and viewing the dashboard                 │
└──────────────────────────────────────────────────────────────────┘
```

## 🔄 Quick Actions Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  User clicks "Manage Employees" Quick Action                     │
└─────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  handleQuickActionClick(action)                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  Check: isAuthenticated?                                   │  │
│  │                                                            │  │
│  │  ┌─────────────────┐         ┌─────────────────┐          │  │
│  │  │  YES (logged in)│         │  NO (not logged)│          │  │
│  │  └────────┬────────┘         └────────┬────────┘          │  │
│  │           │                           │                   │  │
│  │           ▼                           ▼                   │  │
│  │  navigate(action.route)      toast("Login required")     │  │
│  │  → Go to /employees          → Stay on login page        │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

## 🛡️ Protected Route Decision Tree

```
                    User navigates to /employees
                              │
                              ▼
                    ┌──────────────────┐
                    │  ProtectedRoute  │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  isLoading?     │
                    └────┬────────┬───┘
                         │        │
                    YES  │        │  NO
                         │        │
                         ▼        ▼
                  ┌──────────┐   ┌──────────────────┐
                  │ Show     │   │ isAuthenticated? │
                  │ Loading  │   └────┬────────┬────┘
                  └──────────┘        │        │
                                 YES  │        │  NO
                                      │        │
                                      ▼        ▼
                            ┌──────────────┐  ┌──────────────┐
                            │ Has required │  │ Redirect to  │
                            │ role?        │  │ /login       │
                            └────┬────┬────┘  └──────────────┘
                                 │    │
                            YES  │    │  NO
                                 │    │
                                 ▼    ▼
                        ┌──────────┐  ┌──────────────┐
                        │ Render   │  │ Show Access  │
                        │ Content  │  │ Denied       │
                        └──────────┘  └──────────────┘
```

## 🔑 Token Storage & Retrieval

```
┌─────────────────────────────────────────────────────────────────┐
│  Token Lifecycle                                                 │
└─────────────────────────────────────────────────────────────────┘

1. LOGIN
   ├─ Backend generates tokens
   ├─ Frontend receives tokens
   ├─ Store in localStorage
   │  ├─ localStorage.setItem('accessToken', token)
   │  └─ localStorage.setItem('refreshToken', token)
   └─ Update auth state

2. API REQUESTS
   ├─ Axios interceptor reads token
   │  └─ const token = localStorage.getItem('accessToken')
   ├─ Adds to request header
   │  └─ Authorization: Bearer <token>
   └─ Backend validates token

3. TOKEN EXPIRY
   ├─ API returns 401 Unauthorized
   ├─ Interceptor catches error
   ├─ Attempts token refresh
   │  ├─ POST /api/auth/refresh
   │  ├─ Send refreshToken
   │  └─ Receive new accessToken
   ├─ Update localStorage
   └─ Retry original request

4. LOGOUT
   ├─ Call logout API
   ├─ Backend invalidates refreshToken
   ├─ Clear localStorage
   │  ├─ localStorage.removeItem('accessToken')
   │  └─ localStorage.removeItem('refreshToken')
   ├─ Update auth state
   └─ Redirect to /login
```

## ⚡ Race Condition Fix

### BEFORE (Broken)
```
┌─────────────────────────────────────────────────────────────────┐
│  Login Flow - RACE CONDITION                                     │
└─────────────────────────────────────────────────────────────────┘

login() called
    │
    ├─ Store tokens in localStorage ─────┐
    │                                     │ (async)
    ├─ Dispatch LOGIN_SUCCESS ───────────┤
    │                                     │ (async)
    └─ Return { success: true } ─────────┤
                                          │
navigate() called IMMEDIATELY ◄──────────┘
    │
    ▼
ProtectedRoute checks isAuthenticated
    │
    └─ Still FALSE! (state not updated yet)
        │
        └─ Redirect to /login ❌
```

### AFTER (Fixed)
```
┌─────────────────────────────────────────────────────────────────┐
│  Login Flow - FIXED                                              │
└─────────────────────────────────────────────────────────────────┘

login() called
    │
    ├─ Store tokens in localStorage ─────┐
    │                                     │ (completes)
    ├─ Dispatch LOGIN_SUCCESS ───────────┤
    │                                     │ (completes)
    └─ Return { success: true } ─────────┤
                                          │
Wait 100ms ◄──────────────────────────────┘
    │ (ensures state propagation)
    │
navigate() called
    │
    ▼
ProtectedRoute checks isAuthenticated
    │
    └─ TRUE! (state updated)
        │
        └─ Render dashboard ✅
```

## 📱 State Management

```
┌─────────────────────────────────────────────────────────────────┐
│  AuthContext State                                               │
└─────────────────────────────────────────────────────────────────┘

Initial State:
{
  user: null,
  isAuthenticated: false,
  isLoading: true,
  error: null,
  permissions: []
}

After LOGIN_START:
{
  user: null,
  isAuthenticated: false,
  isLoading: true,      ← Changed
  error: null,
  permissions: []
}

After LOGIN_SUCCESS:
{
  user: {               ← Changed
    id: "...",
    name: "Admin User",
    email: "admin@example.com",
    role: "Admin",
    permissions: [...]
  },
  isAuthenticated: true, ← Changed
  isLoading: false,      ← Changed
  error: null,
  permissions: [...]     ← Changed
}

After LOGOUT:
{
  user: null,            ← Reset
  isAuthenticated: false,← Reset
  isLoading: false,
  error: null,
  permissions: []        ← Reset
}
```

---

**Legend:**
- ✅ Success path
- ❌ Error path
- ← Data flow
- ▼ Sequential flow
- ┌─┐ Component/Function boundary
